Place your background track here as `wedding-theme.mp3`.

The path is configured in `src/data/wedding.ts` under `music.src`.
Until a file exists, the floating player shows a disabled "No music" state.
